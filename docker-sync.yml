version: "3"

# define the containers we would like to sync
syncs:
  hugo-sync:
    src: './' # host directory
    dest: '/src' # container directory
    sync_host_ip: 'auto' # possible that it's 0.0.0.0 (test)
    sync_strategy: 'default'

